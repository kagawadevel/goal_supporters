<h1>目標一覧</h1>

<table class="table table-hover">
<% @goals.each do |goal| %>
  <tr>
    <td><div>
      <p><strong>目標名：</strong><%= goal.name %></p>
    </div>
    <div>
      <%= link_to '確認', goal_path(goal.id), class: "btn btn-info" %>
    </div>
    </td>
  </tr>
<% end %>